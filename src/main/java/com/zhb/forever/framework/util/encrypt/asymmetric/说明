
非对称加密算法
两把秘钥，一个公开称为公钥，一个私有私钥

用途：数据保密

解决了对称加密算法私钥分配的问题
安全性高
加密解密速度慢，极端情况比对称加密慢1000倍。
由于非对称加密速度慢，一般很少用它用于数据加密，而是用来加密对称加密中的秘钥
也就是将对称加密和非对称加密结合，用对称加密加密数据，用非对称加密加密对称加密中的秘钥。

主要包含：DH、RSA、ElGamal、ECC
